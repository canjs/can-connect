<person-model getList='{type=type}'>
	{{#isPending}}<span class='pending' ($inserted)="pending"></span>{{/isPending}}
	{{#isResolved}}<span class='resolved' ($inserted)="resolved">{{#each value}}<span>{{id}}</span>{{/each}}</span>{{/isResolved}}
</person-model>
